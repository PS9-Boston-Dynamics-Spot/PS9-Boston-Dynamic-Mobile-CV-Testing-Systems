# Install

- installier Docker / Docker Desktop
- installier WSL

- führe im terminal folgendes aus: 
```bash
docker compose build --no-cache

docker compose up -d

docker compose exec app bash

make run
```