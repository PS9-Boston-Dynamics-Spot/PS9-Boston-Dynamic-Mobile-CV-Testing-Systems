# dataset.yaml
train: ../train
val: ../val
# es werden nur gecroppte Bilder als Trainings- und Validierungsdaten verwendet

# optional, falls du auch test-Daten hast
# test: ../test

# Anzahl der Klassen
nc: 3

# Klassen-Namen (in Reihenfolge ihrer IDs)
names: ['digital_display_ofen', 'analog_display_pressure', 'digital_display_temperature']